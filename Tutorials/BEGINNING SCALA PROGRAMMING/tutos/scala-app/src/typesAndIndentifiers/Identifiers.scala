//Scala allow space inside the vars with backsticks
val `some val with spaces in it` = 100;
println(`some val with spaces in it`);

//You can add special chars like this after underscore
val withSpcialChars_@ = 400;
val specialCharsCanComeAfterUnderscore_? = 300;


//Also it does allow you to use reserved key words
val `import` = 400;
println("Import var value is : " + `import`);

